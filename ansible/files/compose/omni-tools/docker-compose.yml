services:
  omni-tools:
    image: iib0011/omni-tools:latest
    container_name: omni-tools
    restart: unless-stopped
    networks:
      - main_network
    ports:
      - "7071:80"

networks:
  main_network:
    external: true
    name: main_network